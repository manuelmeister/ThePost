{% extends "index.twig" %}

{% block main %}
    <article class="element entry">
        <h2>Error</h2>
        <div class="alert alert-danger" role="alert">
            <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
            <span class="sr-only">Error:</span>
            {{ type }} {{ error.msg }} not found!
        </div>
    </article>
{% endblock %}