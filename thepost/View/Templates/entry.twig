{% extends "index.twig" %}

{% block main %}
    <article class="element entry" id="entry-{{ entry.id }}">
        <h2>{{ entry.title }}</h2>
        <p>{{ entry.content }}</p>
    </article>
{% endblock %}